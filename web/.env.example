# FoodPrint 美食地圖前端（Nuxt）— 連線設定
# 複製成 .env 後填入你自己的值。.env 不會進版控。
#
# 這些是「server 端私有」變數：Nuxt 用它們在 server route（/api/*）代理你的私有 DB
# server，token 永遠不會送到瀏覽器。Nuxt 會自動把 NUXT_ 前綴對應到 runtimeConfig。

# 你的私有 DB server domain（對應 runtimeConfig.foodprintBaseUrl）
# - 跟後端同一台 docker compose 時填 http://api:8000（compose 內網服務名）
# - 獨立部署時填後端對外網址，如 http://192.168.x.x:8000
NUXT_FOODPRINT_BASE_URL=http://192.168.x.x:8000

# 『讀取』token（server 的 FOODPRINT_READ_TOKEN）；server 沒設 token 就留空。
NUXT_FOODPRINT_TOKEN=

# 上公網時：前面有「你自己控制的」反代（Caddy / Cloudflare 等）終止 TLS、且會覆寫
# X-Forwarded-For 時，設成 true，後端 per-IP 限流才看得到真實訪客 IP。
# 留空 / 非 true＝只信任真實 TCP 對端位址（避免訪客自帶假 XFF 繞過限流）。
NUXT_TRUST_PROXY=

# 選用：給整個地圖頁加一道 HTTP Basic Auth（格式 user:pass，例 me:s3cret）。
# 留空＝不設防，任何有網址的人都能瀏覽你的整個美食庫。
# 想公開分享就留空；只給自己 / 少數人看才填。
NUXT_BASIC_AUTH=
